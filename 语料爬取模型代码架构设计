为方便后续拓展，代码分为几层：

sources  解决数据如何爬取的问题。该模块下，每个数据源网站对应一份代码文件。每次新增数据源只需要添加新的代码文件。提供两个主要接口：接受关键词返回所有数据集列表、下载指定的数据集。
policy   在各种限定条件下完成增量爬取的逻辑。例如，每天定时爬取10个数据集增量，需要跳过已经处理过的数据集，排除掉不可用的数据集（大量不相关，广告数据集等），排除掉不可下载的数据集（如需要手动申请权限），排除现在无法处理的数据集（例如多模态数据）。
prescreen  ”预览“分析数据集内容，排除无关数据集。两种方法：用llm判断数据集描述信息、下载小样本数据分析。
data_processing  单独处理用户手动上传的语料数据，支持多种数据格式多种编码。
labeling  对合规数据进行打标签。
storage  对接数据库。将所有爬取记录写入数据表，将所有原始数据存入minio，将清洗后数据写入数据表。
pipeline  编排数据处理流程。需要先读数据库获取爬取情况，进行增量爬取，最后写入数据表。
adapter  提供一个适配层，方便将本模块嵌入大的语料平台。
